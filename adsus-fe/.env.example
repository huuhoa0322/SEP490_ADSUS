# Copy this file to .env.local and adjust if needed.
# .env.local is never committed (see .gitignore).

# ADSUS_BE address. Port 5036 comes from Properties/launchSettings.json ("http" profile).
NEXT_PUBLIC_API_BASE_URL=http://localhost:5036
