# ADSUS — RESTful API Catalog

Full endpoint inventory derived from the PRD (Report 3.0) and the Use Case Specification (Report 3.1), applying the routing/contract rules already ratified in `api_design_rules_v0.2.md`.

| Field                  | Value                                                                                                                      |
| ---------------------- | -------------------------------------------------------------------------------------------------------------------------- |
| Document version       | v1.1 — 2026-07-30                                                                                                          |
| Role                   | Senior API Architect pass — endpoint extraction & contract mapping, not detailed schema design                             |
| Prepared for           | ADSUS (AI Automated Detection and Segmentation of Abnormal Structures Based on Ultrasound Images and Clinical Information) |
| Source — L1/L2 context | `.ai-context/project_context.md`, `.ai-context/api_design_rule/api_design_rules_v0.2.md`                                   |
| Source — business/UC   | `Reports_md/Report_3.0_PRD_ADSUS_md.md` (v1.23) · `Reports_md/Report_3.1_UCS_ADSUS.md` (v1.2, 26/26 UCs + Appendix A)      |
| Coverage               | All 26 UCs (UC-01–UC-26), 40 FTs, 10 modules — every UC maps to ≥1 endpoint below                                          |

> **Source substitution note:** the linked `Report_3.1_UCS_Template_md.md` is the _blank_ UCS template (all fields are literal `{{placeholders}}`, no ADSUS content). The actual filled Use Case Specification — 26/26 UCs, version 1.2, dated 2026-07-23 — lives at `Reports_md/Report_3.1_UCS_ADSUS.md`. This catalog was built from that document instead, since it is the one containing ADSUS's real Main Flows, Business Rules, and Request Fields per UC.

## Changelog v1.0 → v1.1

Senior API Architect review pass, 2026-07-30:

- Added `GET /api/v1/users/me` (#8) — session/profile hydration was missing; needed by UC-01 (post-login) and UC-10 (prefill the edit form).
- Added `POST /api/v1/auth/biometric-devices` (#5) — UC-02's BR-01 requires an explicit device-pairing step that the login endpoint alone cannot perform.
- Split `GET /api/v1/cases?patientProfileId=` (#24) into a staff-only query-param endpoint and a new self-scoped `GET /api/v1/cases/me` (#25) for Patient — the single path previously mixed two structurally different scoping mechanisms under one contract.
- Renamed `PUT /api/v1/reminder-preferences` to `PUT /api/v1/reminder-preferences/{slot}` (#45) — the previous form was the only PUT/PATCH in the catalog with no identifier in the path.
- Endpoint count: 60 → 63 (62 real backend endpoints + 1 N/A row).

## Legend & Notation

**Auth classification** (per `api_design_rules_v0.2.md` §6): `Public` · `Protected` · `Role-restricted (ROLE)` · `N/A`.

**Endpoint type tag:**

- `[CRUD]` — standard resource operation, including state transitions expressed as `PATCH + status field` (preferred over inventing an action verb, per §3.3 of the API rules)
- `[ACTION]` — business logic with no natural REST-CRUD mapping (auth flows, AI inference trigger, PDF generation, external LLM call, aggregated read-only views)
- `[N/A]` — UC has no ADSUS_BE endpoint at all (client-device-only integration)
**Notation:** `{id}` / `{caseId}` = UUID path parameter · query params are `camelCase` · every path starts with the global prefix `/api/v1` · nesting never exceeds 2 levels, matching §3.2 of the API rules. The self-scoped alias `/me` (e.g. `/users/me`, `/cases/me`) denotes "this resource, scoped to the caller's own JWT identity" — a convention used consistently in this catalog though not yet formalized in `api_design_rules_v0.2.md` (see Method & Boundary Notes below).

## API Catalog — Grouped by Module

| #   | Module                   | HTTP Method | Endpoint                                                | Summary                                                                                                                                                                                                                                                                                                                                          | Auth                                                                                  | Actor(s)                                              |
| --- | ------------------------ | ----------- | ------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ------------------------------------------------------------------------------------- | ----------------------------------------------------- |
| 1   | Authentication & Account | POST        | `/api/v1/auth/login`                                    | Authenticate with phone number + password; returns JWT access/refresh tokens and routes the caller to their role-based home area (UC-01). `[ACTION]`                                                                                                                                                                                             | Public                                                                                | All roles                                             |
| 2   | Authentication & Account | POST        | `/api/v1/auth/refresh`                                  | Exchange a valid refresh token for a new access token (UC-01). `[ACTION]`                                                                                                                                                                                                                                                                        | Public                                                                                | All roles                                             |
| 3   | Authentication & Account | POST        | `/api/v1/auth/logout`                                   | Invalidate the current refresh token, ending the signed-in session (UC-01). `[ACTION]`                                                                                                                                                                                                                                                           | Protected                                                                             | All roles                                             |
| 4   | Authentication & Account | POST        | `/api/v1/auth/biometric-login`                          | Sign in via a device-paired biometric credential, valid only after enrollment through `biometric-devices` below (UC-02, _planned_). `[ACTION]`                                                                                                                                                                                           | Public\*                                                                              | Patient (\*device-pairing credential, not a user JWT) |
| 5   | Authentication & Account | POST        | `/api/v1/auth/biometric-devices`                        | Enroll (pair) the current device for biometric sign-in, callable only from an active password-authenticated session; stores the device credential and sets `Users.BiometricEnabled = true` — the prerequisite step UC-02's BR-01 requires, which `biometric-login` alone cannot perform (UC-02). `[ACTION]` `[NEW in v1.1]`                      | Protected                                                                             | Patient                                               |
| 6   | Authentication & Account | POST        | `/api/v1/password-reset-requests`                       | Self-service "Forgot password": validates phone + email match, emails a temporary password, flags a forced password change on next sign-in (UC-03, FT-06). `[ACTION]`                                                                                                                                                                            | Public                                                                                | All roles                                             |
| 7   | Authentication & Account | POST        | `/api/v1/users/{id}/reset-password`                     | Admin-triggered fallback reset for another account when the holder cannot access email; temp password is emailed, never shown on screen (UC-03 AF-02). `[ACTION]`                                                                                                                                                                                | Role-restricted (Admin)                                                               | Admin                                                 |
| 8   | Authentication & Account | GET         | `/api/v1/users/me`                                      | Read the signed-in user's own full profile (full name, phone, email, date of birth, role) — hydrates the session after login/page-reload and prefills the edit form (UC-01, UC-10). `[CRUD]` `[NEW in v1.1]`                                                                                                                                     | Protected                                                                             | All roles (own profile)                               |
| 9   | Authentication & Account | PATCH       | `/api/v1/users/me`                                      | Update the signed-in user's own contact profile (full name, email, date of birth); phone number is immutable, it is the sole login identifier (UC-10). `[CRUD]`                                                                                                                                                                                  | Protected                                                                             | All roles (own profile)                               |
| 10  | Authentication & Account | PATCH       | `/api/v1/users/me/password`                             | Change the signed-in user's own password (current + new + confirmation) (UC-25). `[CRUD]`                                                                                                                                                                                                                                                        | Protected                                                                             | All roles                                             |
| 11  | User & Role Management   | GET         | `/api/v1/users`                                         | List/search login accounts, paginated, for the Admin User List screen (UC-04). `[CRUD]`                                                                                                                                                                                                                                                          | Role-restricted (Admin)                                                               | Admin                                                 |
| 12  | User & Role Management   | POST        | `/api/v1/users`                                         | Create a Doctor / Nurse / Patient login account; system auto-generates and emails a temporary password (UC-04). `[CRUD]`                                                                                                                                                                                                                         | Role-restricted (Admin)                                                               | Admin                                                 |
| 13  | User & Role Management   | GET         | `/api/v1/users/{id}`                                    | View one account's admin-facing profile; `DateOfBirth` is omitted when the target's role is Patient (sensitive_data_rules §5) (UC-04). `[CRUD]`                                                                                                                                                                                                  | Role-restricted (Admin)                                                               | Admin                                                 |
| 14  | User & Role Management   | PATCH       | `/api/v1/users/{id}`                                    | Update an account's role or status, including Lock ⇄ Unlock via `{"status":"LOCKED"` \| `"ACTIVE"}` (UC-04). `[CRUD]`                                                                                                                                                                                                                            | Role-restricted (Admin)                                                               | Admin                                                 |
| 15  | User & Role Management   | DELETE      | `/api/v1/users/{id}`                                    | Soft-delete: deactivate an account (`Status = DEACTIVATED`), one-way, never hard-deleted (UC-04). `[CRUD]`                                                                                                                                                                                                                                       | Role-restricted (Admin)                                                               | Admin                                                 |
| 16  | Dashboard & Reporting    | GET         | `/api/v1/dashboard/statistics?from=&to=`                | Aggregated, anonymized operational KPIs (accounts, cases, AI confirm/reject ratio, appointments, adherence rate), filterable by date range (UC-05). `[ACTION]`                                                                                                                                                                                   | Role-restricted (Admin)                                                               | Admin                                                 |
| 17  | Medical Record           | POST        | `/api/v1/patient-profiles`                              | Create a patient's baseline medical profile (gender, medical history, allergies), linked 1–1 to an existing Patient account (UC-06). `[CRUD]`                                                                                                                                                                                                    | Role-restricted (Doctor, Nurse)                                                       | Doctor, Nurse                                         |
| 18  | Medical Record           | PUT         | `/api/v1/patient-profiles/{id}`                         | Replace/update the baseline profile (UC-06). `[CRUD]`                                                                                                                                                                                                                                                                                            | Role-restricted (Doctor, Nurse)                                                       | Doctor, Nurse                                         |
| 19  | Medical Record           | GET         | `/api/v1/patient-profiles/{id}`                         | Read one patient's baseline profile (UC-06). `[CRUD]`                                                                                                                                                                                                                                                                                            | Role-restricted (Doctor, Nurse)                                                       | Doctor, Nurse                                         |
| 20  | Medical Record           | POST        | `/api/v1/cases`                                         | Create a new visit (Case) with clinical symptoms and ≥1 ultrasound image (JPEG/PNG, ≤20MB each) in one multipart request; attributes the Case to exactly one responsible Doctor (GB-04) (UC-07). `[CRUD]`                                                                                                                                        | Role-restricted (Doctor, Nurse)                                                       | Doctor, Nurse                                         |
| 21  | Medical Record           | POST        | `/api/v1/cases/{caseId}/ultrasound-images`              | Attach additional ultrasound image(s) to an existing, not-yet-Confirmed Case (UC-07). `[CRUD]`                                                                                                                                                                                                                                                   | Role-restricted (Doctor, Nurse)                                                       | Doctor, Nurse                                         |
| 22  | Medical Record           | GET         | `/api/v1/cases/{caseId}/ultrasound-images`              | List the raw ultrasound images uploaded to a Case (UC-07, UC-08). `[CRUD]`                                                                                                                                                                                                                                                                       | Role-restricted (Doctor, Nurse)                                                       | Doctor, Nurse                                         |
| 23  | Medical Record           | GET         | `/api/v1/cases/{id}`                                    | Full record for Doctor/Nurse (raw images, AI Findings, all statuses, progression); the same path returns only the doctor-confirmed conclusion + prescription when called by the owning Patient (GB-05) (UC-08). `[CRUD]`                                                                                                                         | Role-restricted (Doctor, Nurse, Patient-own)                                          | Doctor, Nurse, Patient                                |
| 24  | Medical Record           | GET         | `/api/v1/cases?patientProfileId={id}`                   | List a given patient's visits for Doctor/Nurse (Web SCR-12); `patientProfileId` is required — Patient no longer calls this path (see `/cases/me` below) (UC-08). `[CRUD]`                                                                                                                                                                        | Role-restricted (Doctor, Nurse)                                                       | Doctor, Nurse                                         |
| 25  | Medical Record           | GET         | `/api/v1/cases/me`                                      | List the signed-in Patient's own visits, auto-scoped to Confirmed-only (Mobile SCR-13) — split out from #24 above so that one path no longer mixes an explicit staff query param with an implicit self-scope for Patient; mirrors the `/me` convention from Module 1 (UC-08). `[CRUD]` `[NEW in v1.1]`                                           | Role-restricted (Patient)                                                             | Patient                                               |
| 26  | Medical Record           | GET         | `/api/v1/patients?search=&visitStatus=&page=&pageSize=` | Search/filter the clinical patient list by name, phone, or code, with a visit-status filter — distinct DTO from Admin's `/users` (no account/status fields) (UC-09). `[CRUD]`                                                                                                                                                                    | Role-restricted (Doctor, Nurse)                                                       | Doctor, Nurse                                         |
| 27  | Medical Record           | GET         | `/api/v1/cases/{id}/report`                             | Generate and return the PDF visit report — Confirmed conclusion + prescription only, never raw AI confidence/unreviewed data (UC-12). `[ACTION]`                                                                                                                                                                                                 | Role-restricted (Doctor, Nurse, Patient-own)                                          | Doctor, Nurse, Patient                                |
| 28  | AI Diagnosis (Core)      | POST        | `/api/v1/cases/{caseId}/ai-results`                     | Run AI analysis (CLAHE preprocessing + model inference) on the Case's images; creates a new AI Result at `PENDING_REVIEW` and moves the Case to `ANALYZED` (UC-19). `[ACTION]`                                                                                                                                                                   | Role-restricted (Doctor)                                                              | Doctor                                                |
| 29  | AI Diagnosis (Core)      | GET         | `/api/v1/cases/{caseId}/ai-results`                     | List all AI Result runs for a Case, including their AI Findings (segmentation mask, classification, confidence, size) (UC-19). `[CRUD]`                                                                                                                                                                                                          | Role-restricted (Doctor)                                                              | Doctor                                                |
| 30  | AI Diagnosis (Core)      | GET         | `/api/v1/ai-results/{id}`                               | Read one AI Result's full detail (UC-19). `[CRUD]`                                                                                                                                                                                                                                                                                               | Role-restricted (Doctor)                                                              | Doctor                                                |
| 31  | AI Diagnosis (Core)      | PATCH       | `/api/v1/ai-results/{id}`                               | Confirm or reject a `PENDING_REVIEW` AI Result via `{"status":"CONFIRMED"` \| `"REJECTED", "doctorNote":"..."}` — one-way, Doctor-only (GB-01, GB-02) (UC-19). `[CRUD]` | Role-restricted (Doctor) | Doctor |
| 32  | AI Diagnosis (Core)      | GET         | `/api/v1/cases/{caseId}/progress`                       | Read-only prognosis-tracking aggregate: conclusion/lesion trend across the patient's prior visits, no separate stored entity (FT-22) (UC-19). `[ACTION]`                                                                                                                                                                                         | Role-restricted (Doctor)                                                              | Doctor                                                |
| 33  | AI Model Management      | POST        | `/api/v1/ai-model-versions`                             | Register a new AI model version (version code, artifact URI, change description, benchmark metrics) at status `REGISTERED` (UC-20). `[CRUD]`                                                                                                                                                                                                     | Role-restricted (Admin)                                                               | Admin                                                 |
| 34  | AI Model Management      | GET         | `/api/v1/ai-model-versions`                             | List registered model versions with status and metrics (Sensitivity, Accuracy, AUC) (UC-20). `[CRUD]`                                                                                                                                                                                                                                            | Role-restricted (Admin)                                                               | Admin                                                 |
| 35  | AI Model Management      | GET         | `/api/v1/ai-model-versions/{id}`                        | Read one model version's detail/performance metrics (UC-20). `[CRUD]`                                                                                                                                                                                                                                                                            | Role-restricted (Admin)                                                               | Admin                                                 |
| 36  | AI Model Management      | PATCH       | `/api/v1/ai-model-versions/{id}`                        | Activate or roll back a version via `{"status":"ACTIVE"}`; the previously Active version automatically moves to `INACTIVE` (two-way toggle, exception to GB-01) (UC-20). `[CRUD]`                                                                                                                                                                | Role-restricted (Admin)                                                               | Admin                                                 |
| 37  | Prescription & Adherence | POST        | `/api/v1/prescriptions`                                 | Prescribe medication for a Confirmed Case; ≥1 item, each with ≥1 intake slot (Morning/Noon/Evening) (UC-18). `[CRUD]`                                                                                                                                                                                                                            | Role-restricted (Doctor)                                                              | Doctor                                                |
| 38  | Prescription & Adherence | GET         | `/api/v1/medicines?search=`                             | Search/autocomplete the shared Medicine catalog while prescribing; an unmatched name is auto-added as a new Medicine record (UC-18). `[CRUD]`                                                                                                                                                                                                    | Role-restricted (Doctor)                                                              | Doctor                                                |
| 39  | Prescription & Adherence | GET         | `/api/v1/patient-profiles/{id}/prescriptions`           | List a patient's prescription history (UC-11). `[CRUD]`                                                                                                                                                                                                                                                                                          | Role-restricted (Doctor, Nurse, Patient-own)                                          | Doctor, Nurse, Patient                                |
| 40  | Prescription & Adherence | GET         | `/api/v1/prescriptions/{id}`                            | Read one prescription's detail with its items (drug, dosage, intake slots, duration) (UC-11). `[CRUD]`                                                                                                                                                                                                                                           | Role-restricted (Doctor, Nurse, Patient-own)                                          | Doctor, Nurse, Patient                                |
| 41  | Prescription & Adherence | GET         | `/api/v1/prescriptions/{id}/intake-logs`                | Chronological dose-by-dose intake timeline plus the computed Adherence Rate % (UC-11). `[CRUD]`                                                                                                                                                                                                                                                  | Role-restricted (Doctor, Nurse, Patient-own)                                          | Doctor, Nurse, Patient                                |
| 42  | Prescription & Adherence | GET         | `/api/v1/medication-intake-logs?status=PENDING`         | List the signed-in Patient's due/pending doses (UC-17). `[CRUD]`                                                                                                                                                                                                                                                                                 | Role-restricted (Patient)                                                             | Patient                                               |
| 43  | Prescription & Adherence | PATCH       | `/api/v1/medication-intake-logs/{id}`                   | Confirm a dose taken via `{"status":"TAKEN"}`; auto-completes the parent Prescription once every scheduled dose is Taken (UC-17). `[CRUD]`                                                                                                                                                                                                       | Role-restricted (Patient)                                                             | Patient                                               |
| 44  | Prescription & Adherence | GET         | `/api/v1/reminder-preferences`                          | Read the signed-in Patient's custom reminder time for every intake slot (UC-17). `[CRUD]`                                                                                                                                                                                                                                                        | Role-restricted (Patient)                                                             | Patient                                               |
| 45  | Prescription & Adherence | PUT         | `/api/v1/reminder-preferences/{slot}`                   | Upsert the custom reminder time for one intake slot, identified in the path (e.g. `.../morning`) — now consistent with the ID-addressed PUT/PATCH convention used everywhere else in this catalog (the v1.0 form had no identifier in the path); applies to every future prescription, not tied to one order (UC-17). `[CRUD]` `[FIXED in v1.1]` | Role-restricted (Patient)                                                             | Patient                                               |
| 46  | Appointment Scheduling   | POST        | `/api/v1/schedule-slots`                                | Publish a new `OPEN` schedule slot (date, time range, doctor); rejects an overlapping time range for the same doctor (UC-15). `[CRUD]`                                                                                                                                                                                                           | Role-restricted (Doctor, Nurse)                                                       | Doctor, Nurse                                         |
| 47  | Appointment Scheduling   | GET         | `/api/v1/schedule-slots?doctorId=&slotDate=&status=`    | List/filter schedule slots — used both by Doctor/Nurse managing the calendar and by Patient browsing available slots to book (UC-13, UC-15). `[CRUD]`                                                                                                                                                                                            | Role-restricted (Doctor, Nurse, Patient)                                              | Doctor, Nurse, Patient                                |
| 48  | Appointment Scheduling   | PATCH       | `/api/v1/schedule-slots/{id}`                           | Close a slot via `{"status":"CLOSED"}` — terminal, no reopen; slot status is never affected by booking count (no Capacity concept) (UC-15). `[CRUD]`                                                                                                                                                                                             | Role-restricted (Doctor, Nurse)                                                       | Doctor, Nurse                                         |
| 49  | Appointment Scheduling   | GET         | `/api/v1/schedule-slots/{id}/appointments`              | List the bookings on one slot, so Doctor/Nurse can see affected patients before closing it (UC-15 AF-02). `[CRUD]`                                                                                                                                                                                                                               | Role-restricted (Doctor, Nurse)                                                       | Doctor, Nurse                                         |
| 50  | Appointment Scheduling   | POST        | `/api/v1/appointments`                                  | Book an `OPEN` slot; rejects if the Patient already holds another `BOOKED` appointment in the same window (BR — one active booking per patient per slot) (UC-13). `[CRUD]`                                                                                                                                                                       | Role-restricted (Patient)                                                             | Patient                                               |
| 51  | Appointment Scheduling   | POST        | `/api/v1/appointments/{id}/cancel`                      | Cancel a Booked appointment; requires a mandatory `cancellationReason` field — kept as an action endpoint since a bare status flip cannot express the required-reason gate (UC-14). `[ACTION]`                                                                                                                                                   | Role-restricted (Patient)                                                             | Patient                                               |
| 52  | Appointment Scheduling   | GET         | `/api/v1/appointments/{id}`                             | Read one of the signed-in Patient's own appointments (UC-14). `[CRUD]`                                                                                                                                                                                                                                                                           | Role-restricted (Patient)                                                             | Patient                                               |
| 53  | Appointment Scheduling   | GET         | `/api/v1/appointments?status=`                          | List the signed-in Patient's own appointment history (Booked/Cancelled) (UC-13, UC-14). `[CRUD]`                                                                                                                                                                                                                                                 | Role-restricted (Patient)                                                             | Patient                                               |
| 54  | Appointment Scheduling   | —           | N/A — client-device-only                                | Device-calendar sync is one-way, no read-back: the Mobile App calls the OS Calendar API (API-02) directly. ADSUS_BE exposes **no endpoint** for this integration — inventing one would add scope not in the UCS (UC-16). `[N/A]`                                                                                                                 | N/A                                                                                   | Patient (client-side only)                            |
| 55  | Health Monitoring        | POST        | `/api/v1/health-logs`                                   | Log a daily exercise/diet entry; same-day entries accumulate rather than overwrite (UC-21). `[CRUD]`                                                                                                                                                                                                                                             | Role-restricted (Patient)                                                             | Patient                                               |
| 56  | Health Monitoring        | GET         | `/api/v1/health-logs?date=`                             | Read a day's log entries, backing both the in-app screen (SCR-23) and the home-screen widget (SCR-24) (UC-21). `[CRUD]`                                                                                                                                                                                                                          | Role-restricted (Patient)                                                             | Patient                                               |
| 57  | Engagement               | POST        | `/api/v1/service-feedback`                              | Submit a 1–5 star rating with an optional free-text comment (UC-22). `[CRUD]`                                                                                                                                                                                                                                                                    | Role-restricted (Patient)                                                             | Patient                                               |
| 58  | Engagement               | GET         | `/api/v1/blog-posts?category=`                          | List blog posts; a Patient caller sees `PUBLISHED` only, an Admin caller additionally sees `DRAFT` (GB-05) — same endpoint, role-filtered result set (UC-23, UC-24). `[CRUD]`                                                                                                                                                                    | Protected†                                                                            | Patient, Admin                                        |
| 59  | Engagement               | GET         | `/api/v1/blog-posts/{id}`                               | Read one post's full content, subject to the same Published/Draft visibility rule (UC-23, UC-24). `[CRUD]`                                                                                                                                                                                                                                       | Protected†                                                                            | Patient, Admin                                        |
| 60  | Engagement               | POST        | `/api/v1/blog-posts`                                    | Create a new post at status `DRAFT` (UC-24). `[CRUD]`                                                                                                                                                                                                                                                                                            | Role-restricted (Admin)                                                               | Admin                                                 |
| 61  | Engagement               | PATCH       | `/api/v1/blog-posts/{id}`                               | Edit content, or publish via `{"status":"PUBLISHED"}` (requires title + content, sets publish date, one-way per GB-01) (UC-24). `[CRUD]`                                                                                                                                                                                                         | Role-restricted (Admin)                                                               | Admin                                                 |
| 62  | Engagement               | POST        | `/api/v1/chat-messages`                                 | Ask the AI assistant a general health question; forwards context to the external LLM (API-03), appends the mandatory GB-02 disclaimer, and persists the Q&A turn (UC-26). `[ACTION]`                                                                                                                                                             | Role-restricted (Patient)                                                             | Patient                                               |
| 63  | Engagement               | GET         | `/api/v1/chat-messages?page=&pageSize=`                 | Read the signed-in Patient's own saved AI Chat History (UC-26). `[CRUD]`                                                                                                                                                                                                                                                                         | Role-restricted (Patient)                                                             | Patient                                               |

† "Protected" here means any authenticated role may call it; the response set itself is filtered by role (Draft visibility), not gated by a role check on the endpoint.

## Endpoint Count per Module

| #         | Module                   | Endpoints | CRUD   | ACTION | N/A   |
| --------- | ------------------------ | --------- | ------ | ------ | ----- |
| 1         | Authentication & Account | 10        | 3      | 7      | 0     |
| 2         | User & Role Management   | 5         | 5      | 0      | 0     |
| 3         | Dashboard & Reporting    | 1         | 0      | 1      | 0     |
| 4         | Medical Record           | 11        | 10     | 1      | 0     |
| 5         | AI Diagnosis (Core)      | 5         | 3      | 2      | 0     |
| 6         | AI Model Management      | 4         | 4      | 0      | 0     |
| 7         | Prescription & Adherence | 9         | 9      | 0      | 0     |
| 8         | Appointment Scheduling   | 9         | 7      | 1      | 1     |
| 9         | Health Monitoring        | 2         | 2      | 0      | 0     |
| 10        | Engagement               | 7         | 5      | 2      | 0     |
| **Total** |                          | **63**    | **48** | **14** | **1** |

## Cross-Module Notes

- **User identity is shared, DTOs are not.** `/api/v1/users/*` (Module 2, Admin account management) and `/api/v1/users/me` (Module 1, self-service) both read/write the same `Users` table, but never the same DTO shape — the Admin-facing DTO omits `DateOfBirth` for Patient targets and never exposes password fields; the self-service DTO exposes only the caller's own editable fields.
- **The `/me` alias is now used in two modules — `/users/me` (Module 1) and `/cases/me` (Module 4)** — as a deliberate, consistent convention for "this resource, scoped to the caller's own JWT identity," introduced in this v1.1 pass. It is not yet formalized in `api_design_rules_v0.2.md`; recommend adding a short clause for it alongside §3.2's plural-noun rule.
- **`/api/v1/cases/{id}` is intentionally reused across UC-08 (Medical Record), UC-19 (AI Diagnosis), and UC-12 (PDF export)** rather than duplicated per UC — a Case aggregates Ultrasound Images, AI Results, and the eventual Prescription, so one detail endpoint naturally serves all three read-heavy UCs, filtered by the caller's role. The list-level access, however, is now split by actor (#24 staff, #25 Patient) rather than overloaded on one path — see the v1.1 changelog.
- **`/api/v1/patient-profiles/{id}/prescriptions` (Module 7) nests under a Module 4 resource** — Prescription history is conceptually part of a patient's medical record even though its CRUD lifecycle (Active → Completed) is owned by the Prescription & Adherence module.
- **`/api/v1/schedule-slots` (Module 8) is read by three different actors with three different intents**: Doctor/Nurse manage capacity (UC-15), Patient browses to book (UC-13), and the same nested `/appointments` sub-resource lets Doctor/Nurse see who is affected before closing a slot (UC-15 AF-02).
- **`/api/v1/blog-posts` (Module 10) is the single source for both UC-23 (Patient reads) and UC-24 (Admin authors)** — GB-05's "Published-only for Patient" rule is enforced as a query filter inside this one endpoint, not as two separate endpoints. Unlike the Module 4 case above, this is a pure visibility filter on an otherwise identical scoping mechanism (both callers query "all posts"), so it was kept as one endpoint rather than split.
- **AI Model activation (Module 6) has a downstream effect on Module 5**: `PATCH /api/v1/ai-model-versions/{id}` changing a version to `ACTIVE` is what `POST /api/v1/cases/{caseId}/ai-results` (UC-19) actually invokes at inference time — there is no separate "current model" endpoint; the Active version is looked up server-side. Nesting the AI Diagnosis endpoints under `/cases` is a URL-level convenience only — it does not imply the AI Diagnosis module's Controller/BLL code must live inside the Medical Record module in the Modular Monolith (ADR-001).
- **Prescribing (Module 7, UC-18) depends on Module 5's outcome**: `POST /api/v1/prescriptions` is only valid once the target Case's `PATCH /api/v1/ai-results/{id}` confirmation has moved the Case to `CONFIRMED` (BR-04 of UC-18).
- **`POST /api/v1/chat-messages` (Module 10, UC-26) carries a soft dependency on Module 4**: the UCS's own Request Fields for UC-26 include an optional "Context Case ID" so the assistant can explain a Confirmed conclusion — omitted from this catalog's summary text (per the "no request/response body" constraint) but worth surfacing here since it is a real Engagement → Medical Record coupling.

## Explicit Exclusions (kept out of scope, per the source documents)

- No endpoint for Grad-CAM/heatmap explanation — descoped in PRD §4.1x / §6.3; Doctors still see segmented regions via AI Findings (existing `ai-results` endpoints already cover this).
- No endpoint for national e-ID (CCCD/BHYT/VNeID), payment/insurance, or HIS/PACS integration — explicitly out of scope (PRD §6.3).
- No SMS or business-notification-Email endpoints — Push (API-01) is the only in-app notification channel; Email is scoped solely to the password-reset flow (endpoint #6 above).
- No endpoint invented for "processing_ms" or a per-run AI timing record — PRD §6.1 confirms the <2s target is verified via testing/APM, not stored per inference.
- No hard-delete endpoint exists anywhere in this catalog for medical entities (Case, AI Result, Prescription, Appointment, Ultrasound Image) — consistent with GB-03; only `DELETE /api/v1/users/{id}` exists, and it performs a soft-delete (see `<http_status_codes>` in `api_design_rules_v0.2.md`).
- UC-02 (Biometric login) is retained as _planned/Could-priority_ per FT-03 — included in the catalog for completeness, not a claim that it is implemented.

## Method & Boundary Notes

- Every state-transition UC (AI Result confirm/reject, Model Version activate/rollback, Schedule Slot close, Account lock/unlock, Blog Post publish, Intake Log confirm) uses `PATCH + status field` rather than an invented action verb — per §3.3 of `api_design_rules_v0.2.md`, action-style `POST .../verb` is reserved only for transitions with no natural REST mapping.
- The exceptions that do use an action-style endpoint are `POST /appointments/{id}/cancel` (the mandatory `cancellationReason` gate is not a plain field update), `POST /auth/biometric-devices` (a security enrollment step, not a field update), and the Auth module's `login`/`refresh`/`logout`/`biometric-login` (no underlying resource to PATCH).
- Reschedule (UC-14 AF-01) has **no dedicated endpoint** — the UCS explicitly models it as `POST /appointments/{id}/cancel` followed by a fresh `POST /appointments`, matching BR-04's "cancel old + book new, both kept" rule.
- All list endpoints follow the mandatory pagination contract (`page`, `pageSize`, wrapped in `PagedResult<T>`) per §7.1 of the API rules — omitted from the Endpoint column above for brevity except where a UC-specific filter is the point of interest.
- **Naming rule tension (unresolved, flagged for the API rules document, not fixed here):** `service-feedback` (#57) and `.../progress` (#32) are uncountable/mass nouns that cannot be meaningfully pluralized, which sits at odds with §3.2's literal "always plural nouns" wording. This catalog keeps them singular rather than forcing an awkward plural ("service-feedbacks", "progresses"); `api_design_rules_v0.2.md` should gain an explicit carve-out for this case rather than leave it as a silent, undocumented deviation.
- **Data-model gap inherited from the UCS, not introduced here:** the `category` query param on `GET /blog-posts` (#58) traces back to UC-23's own `GetBlogArticlesRequest` Request Fields table, but the Blog Post entity in PRD §2.2.o has no corresponding stored attribute (only title, content, status, publish date). This should be raised back to whoever owns the PRD/UCS, not silently resolved at the API layer.

---

Generated for ADSUS · sources: `Report_3.0_PRD_ADSUS_md.md` (v1.23) · `Report_3.1_UCS_ADSUS.md` (v1.2) · `project_context.md` · `api_design_rules_v0.2.md`
This catalog extracts and organizes endpoints already implied by ratified project documents — it does not introduce new business features.

> **Note (updated 2026-07-31):** the physical database schema (`ADSUS_Physical_Schema_PostgreSQL_v1.sql`) may still change before implementation — e.g. resolving the `slot_status` `FULL` vs. the Open/Closed-only decision already recorded in `Report_3.1_UCS_ADSUS.md`. (The `NURSE` value in `user_role` — originally flagged missing here on 2026-07-30 — was actually already added on 2026-07-29; confirmed in `ADSUS_BE.DAL/Entities/Enums.cs`/`AppDbContext.cs`.) This catalog's endpoint list should be revisited and adjusted whenever the underlying schema changes, rather than treated as permanently fixed.
